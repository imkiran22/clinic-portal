// Placeholder until `supabase gen types typescript` is run against a real project.
// Replace this file with the generated output after applying migrations.
export type Database = Record<string, unknown>
